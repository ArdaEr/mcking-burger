import React from 'react';


import { Wrapper, Title} from './Header.styles';

const Header = (props) => {
  return (
    <Wrapper>
      <Title>McKing</Title>
     
    </Wrapper>
  );
};

export default Header;
